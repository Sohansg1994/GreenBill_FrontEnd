import * as React from 'react';

import AppAppBar from './modules/views/AppAppBar';
import withRoot from './modules/withRoot';

function Calculations() {
  return (
    <React.Fragment>
      <AppAppBar/>
      
    
    </React.Fragment>
  );
}

export default withRoot(Calculations);
